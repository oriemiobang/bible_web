{
  "scripts": {
    "start": "node server.js",
    "build": "npm rebuild bcrypt --build-from-source && npm install",
    "postinstall": "node -e \"console.log('Postinstall script running')\"",
    "clean": "rimraf node_modules package-lock.json && npm install"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.9.1",
    "validator": "^13.12.0"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
